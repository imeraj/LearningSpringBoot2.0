server:
  port: 8200

spring:
  cloud:
    stream:
      bindings:
        input:
          destination: lombok-chat
          group: comments-chat
          content-type: application/json
        newComments:
          